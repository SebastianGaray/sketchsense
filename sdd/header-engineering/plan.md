# Header and engineering process plan

- Refactor `DemoShell` so project navigation is part of the sticky header rather than a detached sidebar.
- Anchor the theme disclosure to the utility edge and constrain its menu to the viewport.
- Add a localized, static engineering page using the existing editorial components and direct links to versioned SDD evidence.
- Extend Playwright coverage and run formatting, linting, type checking, unit tests, build, browser tests, and dependency audit.
