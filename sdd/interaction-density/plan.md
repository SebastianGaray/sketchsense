# Interaction density plan

- Reduce `Experience` copy to labels and actionable state.
- Add one compact live top-result region before the input controls and synchronize it with inference.
- Toggle the freehand-only stroke control from the existing mode state.
- Update styling, design documentation, and browser regressions.
- Run the complete web validation matrix and inspect desktop and mobile layouts.
