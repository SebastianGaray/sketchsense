---
import type { Language } from '../content';
import DemoShell from './DemoShell.astro';
import Experience from './Experience.astro';

interface Props {
  lang: Language;
}

const { lang } = Astro.props;
---

<DemoShell lang={lang} active="draw">
  <Experience lang={lang} />
</DemoShell>
